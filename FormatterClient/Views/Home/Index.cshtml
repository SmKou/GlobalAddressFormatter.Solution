@{
    ViewData["Title"] = "GAF";
    Layout = "_Layout";
}

<main>
    <section>
        @using (Html.BeginForm("Index", "Home", FormMethod.Post))
        {
            @Html.Label("CountryCode", "Select a Country:")
            @Html.DropDownList("CountryCode", "CountryCode", new { @onchange = "this.form.submit()"})
        }
    </section>
    <section>
        @* @if (formats != null)
        {
            <table>
                <thead>
                    <tr>
                        <th>Format</th>
                        <th>Pattern</th>
                        <th>Description</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (Format format in @formats)
                    {
                        <tr>
                            <button id="@format.Name" @onclick="DisplayForm">
                                <td>@format.Name</td>
                                <td>@format.Pattern</td>
                                <td>@format.Description</td>
                            </button>
                        </tr>
                    }
                </tbody>
            </table>
        } *@
    </section>
    <section></section>
</main>

@* @code {
    private Country country;
    public List<Format> formats;

    private void Get(ChangeEventArgs e)
    {
        string code = e.Value.ToString();
        country = ViewBag.Countries.SingleOrDefault(entry => entry.CountryCode == code);
        formats = Format.GetFormats(country);
    }
} *@